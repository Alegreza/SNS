services:
  - type: web
    name: kobe
    runtime: node
    buildCommand: npm run install:server
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 20
      - key: JWT_SECRET
        generateValue: true
      - key: PORT
        value: 3000
