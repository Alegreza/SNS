/**
 * Auth config: Google Client ID, admin email for manual verification.
 * Set GOOGLE_CLIENT_ID in server/config.js (or .env) for Google sign-in.
 */
window.AUTH_CONFIG = {
  adminEmail: "mkim28@cranbrook.edu",
  googleClientId: "" // Set if using Google sign-in; must match server config
};
